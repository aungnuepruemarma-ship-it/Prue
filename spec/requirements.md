# Requirements

- The runtime must be modular.
- Every committed transformation must pass Φ.
- Every entity must preserve provenance.
- Memory must support working, episodic, semantic, and skill layers.
- Tool/model routing must happen through a capability router.
- All changes must be versioned and testable.
